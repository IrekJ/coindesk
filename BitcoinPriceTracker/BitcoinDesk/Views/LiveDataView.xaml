<UserControl x:Class="BitcoinDesk.Views.LiveDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BitcoinDesk.Views"
             xmlns:converters="clr-namespace:BitcoinDesk.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <Button Content="Refresh Data" Command="{Binding RefreshDataCommand}" Width="150" Margin="0,0,0,10" />
            <Button Content="Save Data" Command="{Binding SaveDataCommand}" Width="150" Margin="0,0,0,10" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="Gray" Margin="0,0,0,5" />
            <TextBlock Text="Current Price (CZK):" FontWeight="Bold" />
            <TextBlock Text="{Binding CurrentPriceCZK, StringFormat='{}{0:C}'}" FontSize="16" Foreground="DarkGreen" />
            <ProgressBar IsIndeterminate="True" 
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                         Height="5" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
